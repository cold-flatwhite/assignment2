const colors = {
  primary: "#4A3C93",
  secondary: "#D2C7E7",
  danger: "#B20000",
  text: "#333",
  white: "#fff",
  border: "#ccc",
  checkbox : "#4630EB",
};

const fontSizes = {
  extraSmall : 11,
  small: 14,
  medium: 16,
  large: 18,
};

const spacing = {
  extraSmall : 5,
  small: 10,
  medium: 20,
  large: 30,
  extraLarge: 55,
};

const opacity = {
  low: 0.5,
  medium: 0.7,
  high: 1.0,
};

const borderRadius = {
  small: 5,
  medium: 10,
  large: 15,
};

const flex = {
  row: "row",
  column: "column",
};

const flexSize = {
  small : 1,
  medium : 2,
  large : 3,
}

const alignItems = {
  center: "center",
  flexStart: "flex-start",
  flexEnd: "flex-end",
};

const justifyContent = {
  center: "center",
  flexStart: "flex-start",
  flexEnd: "flex-end",
  spaceBetween: "space-between",
};

const textAlign = {
  center: "center",
  left: "left",
  right: "right",
};

const fontWeights = {
  bold: 'bold',
  normal: 'normal',
};

const dimensions = {
  heightSmall: 40,
  heightMedium: 100,
  widthFull: '100%',
};

const borderWidths = {
  thin: 2,
  thick: 4,
};

const zIndex = {
  high: 1000,
  low: 1,
};

const textAlignVertical = {
  top: 'top',
  center: 'center',
  bottom: 'bottom',
};

const themes = {
  commonButtonBackground: "#4A3C93",
  commonButtonText: "#ffffff",
  commonHeaderBackground: "#4A3C93",
  commonHeaderText: "#ffffff",
  lightBackground: "#D2C7E7",
  lightText: "white",
  darkBackground: "#B3A2D0",
  darkText: "black",
};

export default {
  colors,
  fontSizes,
  spacing,
  borderRadius,
  flex,
  alignItems,
  justifyContent,
  textAlign,
  fontWeights,
  zIndex,
  dimensions,
  borderWidths,
  textAlignVertical,
  flexSize,
  opacity,
  themes
};